# jojopa tenant override — REUSE the existing jsec repo (no per-tenant clone).
# The control-plane repo is a shared template; only the DATA dir is isolated per tenant.
# This avoids cloning the same repo repeatedly on one machine.

TENANT_NAME=jojopa
TENANT_ROOT=/Users/pdmba-jasonseah/Documents/jsec
TENANT_DATA=$HOME/hermes-tenants/jojopa/data
TENANT_IMAGE=jojopa-assistant
TENANT_MEM_LIMIT=4g
TENANT_CPU_LIMIT=2
